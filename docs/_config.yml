title: jena-fuseki-spring-boot-starter
description: 让 Spring Boot 项目能像使用 `JdbcTemplate` 操作数据库一样，方便地操作 Apache Jena Fuseki 三元组数据库（知识图谱 / RDF 图库）。
baseurl: "/jena-fuseki-spring-boot-starter"
url: "https://zendodx.github.io/"

# just-the-docs 主题
theme: just-the-docs

# 导航：默认展开所有子菜单，不折叠
nav_fold: false

# 页内目录（右侧 TOC）：自动为 h2/h3 标题生成锚点
heading_anchors: true

# 全文搜索
search_enabled: true
search:
  heading_level: 2
  previews: 3
  preview_words_before: 5
  preview_words_after: 10
  tokenizer_separator: /[\s/]+/
  rel_url: true
  button: false

# 导航辅助链接（右上角）
aux_links:
  "GitHub":
    - "https://github.com/zendodx/jena-fuseki-spring-boot-starter"

aux_links_new_tab: true

# 页脚
footer_content: "Copyright &copy; 2024-2026 jena-fuseki-spring-boot-starter. Distributed under the Apache 2.0 license."

# 代码复制按钮
enable_copy_code_button: true

# 标注块（Callouts）
callouts_level: quiet
callouts:
  highlight:
    color: yellow
  important:
    title: Important
    color: blue
  new:
    title: New
    color: green
  note:
    title: Note
    color: purple
  warning:
    title: Warning
    color: red

# 返回顶部按钮
back_to_top: true
back_to_top_text: "返回顶部"

# Markdown 扩展
kramdown:
  syntax_highlighter_opts:
    block:
      line_numbers: false

# Jekyll 插件
plugins:
  - jekyll-seo-tag

# 排除不需要渲染的目录
exclude:
  - "*.gemspec"
  - "Gemfile"
  - "Gemfile.lock"
  - "node_modules/"
  - "vendor/"
  - "files/"

